[project]
name = "template-ansible"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "aar-doc==2.3.0",
    "ansible-core==2.21.4",
    "ansible-lint==26.8.0",
    "antsibull-docs==2.27.0",
    "pywinrm==0.5.0", # Only needed if you need to manage Windows servers
]
